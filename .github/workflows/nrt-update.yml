name: PipelineWatch NRT update

on:
  schedule:
    # Every 6 hours at minute 15 (00:15, 06:15, 12:15, 18:15 UTC).
    # VIIRS fire data lands every few hours, so sub-daily refresh genuinely
    # improves fire detection latency. SAR (6-12 day revisit) and TROPOMI
    # (1 pass/day) will be no-ops on most runs, which is cheap and harmless.
    - cron: "15 */6 * * *"
  workflow_dispatch:
    inputs:
      days:
        description: "Rolling NRT window in days"
        required: false
        default: "7"

permissions:
  contents: write

jobs:
  update-nrt:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Run NRT update
        env:
          GEE_PROJECT_ID: ${{ secrets.GEE_PROJECT_ID }}
          EE_SERVICE_ACCOUNT: ${{ secrets.EE_SERVICE_ACCOUNT }}
          EE_PRIVATE_KEY: ${{ secrets.EE_PRIVATE_KEY }}
          EE_CREDENTIALS_JSON: ${{ secrets.EE_CREDENTIALS_JSON }}
          PIPELINEWATCH_AOI: niger_delta
        run: |
          DAYS="${{ github.event.inputs.days || '7' }}"
          python modules/m5_nrt_update.py --days "$DAYS"

      - name: Commit NRT artifacts
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "NRT update"
          file_pattern: >
            data/cached/m5_nrt_latest.json
            data/cached/m5_nrt_history.csv
            outputs/m5_nrt_trend.html
